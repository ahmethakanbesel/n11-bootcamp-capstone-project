package com.n11.userservice.common;

/*@Repository
public interface GenericRepository<E extends BaseEntity, Long> extends JpaRepository<E, Long> {
    Page<E> findAllPWithPageable(Pageable pageable);
}
*/

